#include <stdio.h>
#include <stdlib.h>
 
void readMatrixFromFile(char fileName[])
{
    
}
int main()
{
    char fileName[101];
    scanf("%s", fileName);
    readMatrixFromFile(fileName);
    return 0;
}
